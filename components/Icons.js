import {
  Sparkles,
  Rocket,
  AlertTriangle,
  AlignJustify,
  ArrowUpRight,
  BarChart,
  BookCheck,
  BookText,
  Bot,
  BotMessageSquare,
  BrainCircuit,
  Building2,
  CalendarDays,
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  CircleAlert,
  CircleChevronDown,
  CircleMinus,
  CirclePlus,
  CircleUser,
  CircleX,
  ClipboardX,
  Clock,
  Clock10,
  Cog,
  Copy,
  Download,
  Edit,
  Ellipsis,
  EllipsisVertical,
  File,
  FileClock,
  FileSliders,
  FileText,
  Globe,
  Globe2,
  History,
  Home,
  ImageUpIcon,
  Info,
  Key,
  KeyRound,
  LayoutGrid,
  LayoutTemplate,
  LineChart,
  LogOut,
  Mail,
  MessageCircleCode,
  MessageCircleMore,
  Mic,
  MoveDown,
  Parentheses,
  Pause,
  Pencil,
  Play,
  PlugZap,
  Plus,
  Redo,
  RefreshCw,
  Rss,
  Settings2,
  ShieldCheck,
  SquareFunction,
  SquarePen,
  Table,
  TestTube,
  TestTubeDiagonal,
  ThumbsDown,
  ThumbsUp,
  Trash2,
  TriangleAlert,
  Undo,
  User,
  UserPlus,
  Webhook,
  X,
  MessageSquareMore,
  Wand2,
  Settings,
  MonitorPlay,
  Wrench,
  SendHorizontal,
  MessageCircle,
  Blocks,
  ChevronLast,
  CheckCircle,
  MessageSquare,
  Database,
  Shield,
  ExternalLink,
  Upload,
  Link,
  Keyboard,
} from "lucide-react";

/**
 * Monitor play icon showing a monitor with play button
 * Used for tutorial and walkthrough features
 * Style: Blue or purple for interactive features
 * Context: Tutorial sections, walkthrough guides
 */
export const MonitorPlayIcon = (props) => <MonitorPlay {...props} />;

/**
 */
/**
 * Alert icon showing a triangle with an exclamation mark
 * Used for warning notifications and alerts
 * Style: Yellow/orange for warning, red for error
 * Context: Form validation, error messages, warning indicators
 */

/**
 * Wrench icon showing a wrench tool
 * Used for maintenance, configuration, and customization features
 * Style: Blue or gray for neutral settings
 * Context: Configuration panels, maintenance tasks, customization options
 */
export const WrenchIcon = (props) => <Wrench {...props} />;
export const AlertIcon = (props) => <AlertTriangle {...props} />;

/**
 * Message square icon showing a chat bubble
 * Used for messaging and feedback features
 * Style: Blue or gray for neutral messaging
 * Context: Chat features, feedback collection
 */
export const MessageSquareMoreIcon = (props) => <MessageSquareMore {...props} />;

/**
 * Magic wand icon showing a wizard's wand
 * Used for optimization and automation features
 * Style: Blue or purple for magical/optimization actions
 * Context: Optimization tools, automation features
 */
export const Wand2Icon = (props) => <Wand2 {...props} />;

/**
 * Alignment icon showing three horizontal lines
 * Used for alignment and layout controls
 * Style: Blue or gray for layout indicators
 * Context: Layout and alignment features
 */
export const AlignIcon = (props) => <AlignJustify {...props} />;

/**
 * Arrow pointing up and to the right
 * Used for navigation and direction indicators
 * Style: Blue or gray for navigation
 * Context: External links, forward navigation
 */
export const ArrowUpIcon = (props) => <ArrowUpRight {...props} />;

/**
 * Bar chart icon showing graph visualization
 * Used for analytics and statistics
 * Style: Blue or gray for data visualization
 * Context: Analytics dashboards, statistical reports
 */
export const ChartIcon = (props) => <BarChart {...props} />;

/**
 * Checklist icon showing a book with a checkmark
 * Used for task lists and completion indicators
 * Style: Blue or green for task completion
 * Context: Task management, checklist features
 */
export const ChecklistIcon = (props) => <BookCheck {...props} />;

/**
 * Book icon showing text
 * Used for documentation and reading-related features
 * Style: Blue or gray for reading indicators
 * Context: Documentation, reading materials, text content
 */
export const BookIcon = (props) => <BookText {...props} />;

/**
 * Bot icon showing a robot face
 * Used for AI and automation features
 * Style: Blue or purple for AI representation
 * Context: AI chatbots, automation features
 */
export const BotIcon = (props) => <Bot {...props} />;

/**
 * Bot message icon showing a chat bubble with code
 * Used for code-related chat messages
 * Style: Blue or purple for code/chat
 * Context: Code chat messages, AI responses
 */
export const BotMessageIcon = (props) => <BotMessageSquare {...props} />;

/**
 * Brain circuit icon showing neural connections
 * Used for AI and machine learning features
 */
/**
 * Brain circuit icon showing neural connections
 * Used for AI and machine learning features
 * Style: Blue or purple to represent intelligence
 * Context: AI models, neural networks, machine learning
 */
export const BrainIcon = (props) => <BrainCircuit {...props} />;

/**
 * Building icon showing a modern structure
 * Used for organization and workspace features
 * Style: Blue or gray to represent stability
 * Context: Organization management, workspace settings
 */
export const BuildingIcon = (props) => <Building2 {...props} />;

/**
 * Calendar icon showing date selector
 * Used for date-related features and scheduling
 * Style: Blue or gray for neutral date indicators
 * Context: Event scheduling, date pickers, calendar views
 */
export const CalendarIcon = (props) => <CalendarDays {...props} />;

/**
 * Checkmark icon
 * Used for confirmation and success states
 * Style: Green for success, blue for neutral
 * Context: Form validation, task completion, success indicators
 */
export const CheckIcon = (props) => <Check {...props} />;

/**
 * Chevron pointing down
 * Used for dropdown menus and expandable sections
 * Style: Blue or gray for neutral navigation
 * Context: Dropdown menus, expandable content, accordion headers
 */
export const ChevronDownIcon = (props) => <ChevronDown {...props} />;

/**
 * Chevron pointing up
 * Used for collapsing menus and sections
 * Style: Blue or gray for neutral navigation
 * Context: Collapsible menus, closing expanded content
 */
export const ChevronUpIcon = (props) => <ChevronUp {...props} />;

/**
 * Chevron pointing left
 * Used for navigation and back actions
 * Style: Blue or gray for navigation
 * Context: Back navigation, previous actions
 */
export const ChevronLeftIcon = (props) => <ChevronLeft {...props} />;

/**
 * Chevron pointing right
 * Used for forward navigation and next actions
 * Style: Blue or gray for navigation
 * Context: Next actions, forward navigation
 */
export const ChevronRightIcon = (props) => <ChevronRight {...props} />;

/**
 * Circle with alert icon
 * Used for warning notifications in circular form
 * Style: Yellow/orange for warning
 * Context: Warning badges, cautionary indicators
 */
export const CircleAlertIcon = (props) => <CircleAlert {...props} />;

/**
 * Circular chevron pointing down
 * Used for dropdown menus in circular form
 * Style: Blue or gray for navigation
 * Context: Circular dropdown menus, expandable circular sections
 */
export const CircleDownIcon = (props) => <CircleChevronDown {...props} />;

/**
 * Circular minus icon
 * Used for removal actions in circular form
 * Style: Red for deletion, gray for neutral removal
 * Context: Circular remove buttons, deletion actions
 */
export const CircleMinusIcon = (props) => <CircleMinus {...props} />;

/**
 * Circular plus icon
 * Used for addition actions in circular form
 * Style: Green for addition, blue for neutral
 * Context: Add buttons, create actions
 */
export const CirclePlusIcon = (props) => <CirclePlus {...props} />;

/**
 * User icon in circular form
 * Used for user-related features
 * Style: Blue or gray for profile indicators
 * Context: User profiles, avatar placeholders
 */
export const UserCircleIcon = (props) => <CircleUser {...props} />;

/**
 * Close icon in circular form
 * Used for closing actions
 * Style: Red for destructive actions, gray for neutral
 * Context: Close buttons, dismiss actions
 */
export const CloseCircleIcon = (props) => <CircleX {...props} />;

/**
 * Clipboard with X icon
 * Used for clipboard-related actions
 * Style: Blue or gray for clipboard actions
 * Context: Copy/paste operations, clipboard management
 */
export const ClipboardXIcon = (props) => <ClipboardX {...props} />;

/**
 * Clock icon
 * Used for time-related features
 * Style: Blue or gray for time indicators
 * Context: Time displays, schedule management
 */
export const ClockIcon = (props) => <Clock {...props} />;

/**
 * Clock showing 10 o'clock
 * Used for time-related features
 * Style: Blue or gray for time indicators
 * Context: Specific time indicators, scheduling
 */
export const ClockTenIcon = (props) => <Clock10 {...props} />;

/**
 * Settings icon showing gear/cog
 * Used for settings and configuration features
 * Style: Blue or gray for neutral settings
 * Context: Configuration panels, settings menus
 */
export const CogIcon = (props) => <Cog {...props} />;

/**
 * Settings icon showing gear/cog
 * Used for settings and configuration features
 * Style: Blue or gray for neutral settings
 * Context: Configuration panels, settings menus
 */
export const SettingsIcon = (props) => <Settings {...props} />;
/**
 * Copy icon showing clipboard and document
 * Used for copying actions
 * Style: Blue or gray for neutral actions
 * Context: Copy to clipboard, duplicate actions
 */
export const CopyIcon = (props) => <Copy {...props} />;

/**
 * Download icon showing arrow pointing down
 * Used for downloading actions
 * Style: Blue or gray for neutral actions
 * Context: File downloads, data exports
 */
export const DownloadIcon = (props) => <Download {...props} />;

/**
 * Edit icon showing pencil
 * Used for editing and modification actions
 * Style: Blue or purple for editing
 * Context: Edit forms, modify content
 */
export const EditIcon = (props) => <Edit {...props} />;

/**
 * Horizontal ellipsis dots
 * Used for more options and menu indicators
 * Style: Blue or gray for neutral indicators
 * Context: Horizontal menus, options lists
 */
export const EllipsisIcon = (props) => <Ellipsis {...props} />;

/**
 * Vertical ellipsis dots
 * Used for more options in vertical menus
 * Style: Blue or gray for neutral indicators
 * Context: Vertical menus, dropdown lists
 */
export const EllipsisVerticalIcon = (props) => <EllipsisVertical {...props} />;

/**
 * File icon showing document
 * Used for file-related features
 * Style: Blue or gray for neutral file indicators
 * Context: File management, document handling
 */
export const FileIcon = (props) => <File {...props} />;

/**
 * File with clock icon
 * Used for time-stamped files
 * Style: Blue or gray for neutral indicators
 * Context: Timestamped documents, version control
 */
export const FileClockIcon = (props) => <FileClock {...props} />;

/**
 * File with sliders icon
 * Used for file filtering and sorting
 * Style: Blue or gray for neutral indicators
 * Context: File filtering, sorting options
 */
export const FilterSliderIcon = (props) => <FileSliders {...props} />;

/**
 * File with text icon
 * Used for text documents
 * Style: Blue or gray for neutral indicators
 * Context: Text files, document editing
 */
export const FileTextIcon = (props) => <FileText {...props} />;

/**
 * Globe icon showing world map
 * Used for global and world-related features
 * Style: Blue or gray for neutral indicators
 * Context: Global features, international settings
 */
export const GlobeIcon = (props) => <Globe {...props} />;

/**
 * Alternative globe icon showing world map
 * Used for global and world-related features
 * Style: Blue or gray for neutral indicators
 * Context: Global features, international settings
 */
export const GlobeAltIcon = (props) => <Globe2 {...props} />;

/**
 * History icon showing clock
 * Used for history and timeline features
 * Style: Blue or gray for neutral indicators
 * Context: History tracking, version control
 */
export const HistoryIcon = (props) => <History {...props} />;

/**
 * Home icon showing house
 * Used for home and dashboard features
 * Style: Blue or gray for neutral indicators
 * Context: Dashboard, home page navigation
 */
export const HomeIcon = (props) => <Home {...props} />;

/**
 * Image upload icon showing camera and cloud
 * Used for image upload features
 * Style: Blue or gray for neutral indicators
 * Context: Image uploads, media management
 */
export const ImageUploadIcon = (props) => <ImageUpIcon {...props} />;

/**
 * Information icon showing i in circle
 * Used for information and help sections
 * Style: Blue or gray for neutral indicators
 * Context: Help sections, information display
 */
export const InfoIcon = (props) => <Info {...props} />;
/**
 * Key icon showing traditional key
 * Used for authentication and access features
 * Style: Blue or gray for neutral indicators
 * Context: Login screens, access management
 */
export const KeyIcon = (props) => <Key {...props} />;

/**
 * Rounded key icon
 * Used for authentication and access features
 * Style: Blue or gray for neutral indicators
 * Context: Login screens, access management
 */
export const KeyRoundIcon = (props) => <KeyRound {...props} />;
/**
 * Grid layout icon showing grid pattern
 * Used for grid and layout features
 * Style: Blue or gray for neutral indicators
 * Context: Grid layouts, table views
 */
export const LayoutGridIcon = (props) => <LayoutGrid {...props} />;

/**
 * Template layout icon showing template pattern
 * Used for template and layout features
 * Style: Blue or gray for neutral indicators
 * Context: Template management, layout settings
 */
export const TemplateIcon = (props) => <LayoutTemplate {...props} />;

/**
 * Line chart icon showing graph visualization
 * Used for analytics and statistics
 * Style: Blue or gray for data visualization
 * Context: Analytics dashboards, statistical reports
 */
export const LineChartIcon = (props) => <LineChart {...props} />;

/**
 * Logout icon showing door with arrow
 * Used for logout and exit actions
 * Style: Red for destructive actions, gray for neutral
 * Context: User logout, session end
 */
export const LogoutIcon = (props) => <LogOut {...props} />;

/**
 * Mail icon showing envelope
 * Used for email and messaging features
 * Style: Blue or gray for neutral indicators
 * Context: Email features, messaging
 */
export const MailIcon = (props) => <Mail {...props} />;

/**
 * Code message icon showing chat bubble with code
 * Used for code-related chat messages
 * Style: Blue or purple for code/chat
 * Context: Code chat messages, AI responses
 */
export const CodeMessageIcon = (props) => <MessageCircleCode {...props} />;

/**
 * Chat message icon showing chat bubble with more
 * Used for general chat messages
 * Style: Blue or gray for neutral indicators
 * Context: Chat features, message display
 */
export const MessageCircleMoreIcon = (props) => <MessageCircleMore {...props} />;

/**
 * Microphone icon
 * Used for voice and audio features
 * Style: Blue or purple for audio
 * Context: Voice input, audio recording
 */
export const MicIcon = (props) => <Mic {...props} />;

/**
 * Move down icon showing arrow pointing down
 * Used for reordering and moving items down
 * Style: Blue or gray for neutral actions
 * Context: Item reordering, list management
 */
export const MoveDownIcon = (props) => <MoveDown {...props} />;

/**
 * Parentheses icon showing brackets
 * Used for code and text formatting
 * Style: Blue or gray for neutral indicators
 * Context: Code editing, text formatting
 */
export const ParenthesesIcon = (props) => <Parentheses {...props} />;

/**
 * Pause icon showing double bars
 * Used for pausing actions
 * Style: Blue or gray for neutral actions
 * Context: Media controls, pause operations
 */
export const PauseIcon = (props) => <Pause {...props} />;

/**
 * Pencil icon
 * Used for writing and editing actions
 * Style: Blue or purple for editing
 * Context: Edit forms, modify content
 */
export const PencilIcon = (props) => <Pencil {...props} />;

/**
 * Play icon showing triangle
 * Used for starting actions
 * Style: Blue or green for start actions
 * Context: Media controls, start operations
 */
export const PlayIcon = (props) => <Play {...props} />;

/**
 * Plug icon with lightning
 * Used for connection and power features
 * Style: Blue or purple for connection
 * Context: API integrations, power features
 */
export const PlugIcon = (props) => <PlugZap {...props} />;

/**
 * Plus icon
 * Used for addition actions
 * Style: Green for addition, blue for neutral
 * Context: Add buttons, create actions
 */
export const AddIcon = (props) => <Plus {...props} />;

/**
 * Redo icon showing arrow pointing right
 * Used for repeating actions
 * Style: Blue or gray for neutral actions
 * Context: Redo operations, repeat actions
 */
export const RedoIcon = (props) => <Redo {...props} />;

/**
 * Refresh icon showing circular arrow
 * Used for refresh and reload actions
 * Style: Blue or gray for neutral actions
 * Context: Refresh operations, reload data
 */
export const RefreshIcon = (props) => <RefreshCw {...props} />;

/**
 * RSS icon showing RSS feed symbol
 * Used for RSS feed features
 * Style: Orange for RSS standard
 * Context: RSS feed subscriptions, feed management
 */
export const RSSIcon = (props) => <Rss {...props} />;
/**
 * Alternative settings icon showing gear/cog
 * Used for settings and configuration features
 * Style: Blue or gray for neutral settings
 * Context: Configuration panels, settings menus
 */
export const SettingsAltIcon = (props) => <Settings2 {...props} />;

/**
 * Security icon showing shield with check
 * Used for security and protection features
 * Style: Blue or green for security
 * Context: Security settings, protection features
 */
/**
 * Security icon showing shield with check
 * Used for security and protection features
 * Style: Blue or green for security
 * Context: Security settings, protection features
 */
export const SecurityIcon = (props) => <ShieldCheck {...props} />;

/**
 * Function icon showing square with function symbol
 * Used for mathematical and functional features
 * Style: Blue or purple for mathematical operations
 * Context: Calculations, function operations
 */
export const SquareFunctionIcon = (props) => <SquareFunction {...props} />;

/**
 * Square pen icon
 * Used for writing and editing actions
 * Style: Blue or purple for editing
 * Context: Edit forms, modify content
 */
export const SquarePenIcon = (props) => <SquarePen {...props} />;

/**
 * Table icon showing table grid
 * Used for table and data grid features
 * Style: Blue or gray for neutral indicators
 * Context: Data tables, grid views
 */
export const TableIcon = (props) => <Table {...props} />;

/**
 * Test tube icon showing vertical test tube
 * Used for testing and experimentation features
 * Style: Blue or gray for neutral indicators
 * Context: Testing features, experiments
 */
export const TestTubeIcon = (props) => <TestTube {...props} />;

/**
 * Diagonal test tube icon
 * Used for testing and experimentation features
 * Style: Blue or gray for neutral indicators
 * Context: Testing features, experiments
 */
export const TestTubeDiagonalIcon = (props) => <TestTubeDiagonal {...props} />;

/**
 * Thumbs down icon
 * Used for negative feedback and disapproval
 * Style: Red for negative feedback
 * Context: Rating systems, feedback collection
 */
export const ThumbsDownIcon = (props) => <ThumbsDown {...props} />;

/**
 * Thumbs up icon
 * Used for positive feedback and approval
 * Style: Green for positive feedback
 * Context: Rating systems, feedback collection
 */
export const ThumbsUpIcon = (props) => <ThumbsUp {...props} />;

/**
 * Trash icon showing trash can
 * Used for deletion actions
 * Style: Red for destructive actions, gray for neutral
 * Context: Delete operations, remove items
 */
export const TrashIcon = (props) => <Trash2 {...props} />;

/**
 * Triangle alert icon
 * Used for warning states
 * Style: Yellow/orange color
 * Context: Warning messages, cautionary indicators
 */
export const TriangleWarningIcon = (props) => <TriangleAlert {...props} />;
/**
 * Undo icon showing arrow pointing left
 * Used for undoing actions
 * Style: Blue or gray for neutral actions
 * Context: Undo operations, revert changes
 */
export const UndoIcon = (props) => <Undo {...props} />;

/**
 * User icon showing person silhouette
 * Used for user-related features
 * Style: Blue or gray for neutral indicators
 * Context: User profiles, account management
 */
export const UserIcon = (props) => <User {...props} />;

/**
 * Add user icon showing person silhouette with plus
 * Used for adding new users
 * Style: Green for addition, blue for neutral
 * Context: User creation, team management
 */
export const AddUserIcon = (props) => <UserPlus {...props} />;

/**
 * Webhook icon showing connection with lightning
 * Used for webhook and integration features
 * Style: Blue or purple for integration
 * Context: API integrations, webhook configurations
 */
export const WebhookIcon = (props) => <Webhook {...props} />;

/**
 * Close icon showing X
 * Used for closing actions
 * Style: Red for destructive actions, gray for neutral
 * Context: Close buttons, dismiss actions
 */
export const CloseIcon = (props) => <X {...props} />;

export const SendHorizontalIcon = (props) => <SendHorizontal {...props} />;

export const SparklesIcon = (props) => <Sparkles {...props} />;

export const RocketIcon = (props) => <Rocket {...props} />;

export const MessageCircleIcon = (props) => <MessageCircle {...props} />;

export const BlocksIcon = (props) => <Blocks {...props} />;

export const ChevronLastIcon = (props) => <ChevronLast {...props} />;

export const CheckCircleIcon = (props) => <CheckCircle {...props} />;

export const BookOpenIcon = (props) => <BookOpenIcon {...props} />;

export const MessageSquareIcon = (props) => <MessageSquare {...props} />;

export const DatabaseIcon = (props) => <Database {...props} />;

export const ShieldIcon = (props) => <Shield {...props} />;

export const ExternalLinkIcon = (props) => <ExternalLink {...props} />;

export const UploadIcon = (props) => <Upload {...props} />;

export const LinkIcon = (props) => <Link {...props} />;

/**
 * Keyboard icon showing keyboard
 * Used for keyboard shortcuts and input features
 * Style: Blue or gray for neutral indicators
 * Context: Keyboard shortcuts, input methods
 */
export const KeyboardIcon = (props) => <Keyboard {...props} />;

/**
 * X icon showing close/cancel symbol
 * Used for closing and canceling actions
 * Style: Red for destructive actions, gray for neutral
 * Context: Close buttons, cancel actions
 */
export const XIcon = (props) => <X {...props} />;
